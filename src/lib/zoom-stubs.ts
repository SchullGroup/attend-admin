// Empty stub for @zoom/download-manager — a private Zoom internal package
// that is referenced in the UMD bundle but not required for meeting join/video.
// Turbopack resolves this alias so the build succeeds.
export default {};
export const DownloadManager = {};
